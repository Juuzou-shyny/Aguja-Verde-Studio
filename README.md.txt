## Configuración

1. Copia `appsettings.example.json` y renómbralo a `appsettings.json`
2. Rellena los valores con tus credenciales:
   - Cadena de conexión MySQL
   - Clave JWT (mínimo 32 caracteres)
   - Claves de Stripe (obtener en stripe.com)